# This is to test and revise list operations that my dumbass forgot XD
# rows = int(input("Enter number of rows: "))
# columns = int(input("Enter number of columns: "))
# matrix = [["..." for _ in range(columns)] + ["1"] for _ in range(rows)]

matrix = [
    "HE",
    "LX",
    "LOO",
]

# len(matrix[2] > 2):

print(matrix[2])
